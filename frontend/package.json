{
  "scripts": {
      "dev": "vite --host 0.0.0.0 --port 5173",
      "build": "vite build",
      "preview": "vite preview --host 0.0.0.0 --port 5173",
      "start": "npm run build && concurrently \"npm run preview\" \"cd ../backend && venv\\Scripts\\uvicorn app.main:app --host 0.0.0.0 --port 8000\""
    },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "concurrently": "^10.0.4",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
